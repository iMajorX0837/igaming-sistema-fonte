# =============================================================================
# Deploy tenant — Front (build Vite)
# Renomeie para env.front na pasta deploy/tenants/SEU_TENANT/
# Paths relativos = front e API no mesmo domínio (nginx)
# =============================================================================

VITE_API_BASE=/api/supabase
VITE_DEPOSIT_API_BASE=/api/deposit
VITE_PLAYFIVERS_API_BASE=/api/v2
VITE_GAME_LAUNCH_URL=/api/game_launch
