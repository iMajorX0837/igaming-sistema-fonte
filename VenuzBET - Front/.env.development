# =============================================================================
# VenuzBET — Front — só npm run dev
# Proxies definidos em vite.config.ts → PlayFiverAPI local
# =============================================================================

VITE_GAME_LAUNCH_PROXY=http://localhost:3000
VITE_PLAYFIVERS_PROXY=http://localhost:3000
