# =============================================================================
# VenuzBET — Front (Vite) — valores base (dev + produção)
# NÃO commitar. Template: .env.example
# Em dev, .env.development adiciona proxies para localhost:3000
# =============================================================================

# --- API via PlayFiverAPI (credenciais ficam no servidor) ---
VITE_API_BASE=/api/supabase
VITE_DEPOSIT_API_BASE=/api/deposit
VITE_PLAYFIVERS_API_BASE=/api/v2
VITE_GAME_LAUNCH_URL=/api/game_launch

# --- Rodadas grátis / roleta (opcional — padrão: paths relativos) ---
# VITE_FREE_BONUS_URL=/api/free_bonus
# VITE_PRIZE_WHEEL_GRANT_URL=/api/prize_wheel/grant
